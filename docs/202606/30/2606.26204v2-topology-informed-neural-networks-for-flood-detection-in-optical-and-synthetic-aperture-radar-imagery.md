---
title: Topology-Informed Neural Networks for Flood Detection in Optical and Synthetic Aperture Radar Imagery
title_zh: 基于拓扑信息的光学与合成孔径雷达影像洪水检测神经网络
authors: "Sophia Li, Max Zhao, Raghu G. Raj, Tianyu Chen"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.26204v2"
tags: ["query:urban-flood"]
score: 8.0
evidence: 利用神经网络和卫星影像进行洪水检测
tldr: 针对光学卫星图像在云覆盖下难以有效检测洪水的问题，现有深度学习模型虽能利用时序信息提升精度，但作为黑箱难以解释。本文引入拓扑数据分析（TDA），从SEN12-FLOOD图像中提取全局拓扑特征，并将其融入神经网络。实验表明，拓扑描述符独立携带显著的洪水信号，并能与现有网络互补，提升检测的鲁棒性和可解释性。该工作为遥感图像分析提供了一种数学可解释的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26204v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1700, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2606.26204v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1698, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2606.26204v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1769, \"height\": 630}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26204v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1201, \"height\": 463}, {\"url\": \"assets/tables/arxiv/2606.26204v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 812, \"height\": 467}]"
motivation: 现有洪水检测模型缺乏可解释性，在安全关键领域难以信任；拓扑数据分析能捕获全局结构特征，弥补这一缺陷。
method: 从SEN12-FLOOD数据集图像中提取拓扑特征，将其作为额外输入与ResNet-50或ViT等神经网络结合进行洪水检测。
result: 拓扑特征单独即可有效检测洪水，且与神经网络联合使用时，显著提升整体性能与鲁棒性。
conclusion: 拓扑数据分析能增强洪水检测系统的可解释性和鲁棒性，为遥感灾害监测提供新思路。
---

## 摘要
洪水经常影响全球各地。快速准确的洪水检测对于应急响应以及及时减少人员和经济损失至关重要。卫星数据可用性的不断扩大和人工智能的进步增强了对环境危害的监测，但许多洪水事件仍然难以检测，因为云层遮挡了光学卫星图像。Rambour等人引入了SEN12-FLOOD数据集，并使用ResNet-50卷积神经网络骨干提取每幅图像的特征，然后将这些特征输入门控循环单元网络，表明与单幅图像基线相比，时间信息可以显著提高准确性。最近，Chamatidis等人表明，视觉变换器可以在流行的卷积架构上实现强大性能。然而，这些模型通常作为不透明的黑箱运行，使得解释其决策边界、学习特征和内部推理变得困难，特别是在遥感等安全关键领域。相比之下，拓扑数据分析（TDA）为捕捉数据的全局结构特征提供了数学上严谨的框架。TDA已成为分析复杂图像（尤其是具有几何可解释结构的图像，洪水是其中的一个主要候选）的强大工具。在这项工作中，我们使用开源SEN12-FLOOD数据集系统评估了用于洪水检测的拓扑描述符。通过从每幅图像中提取拓扑特征并将其融入神经网络，我们证明了拓扑描述符独立携带有意义的洪水信号，并补充现有网络，从而产生更鲁棒且可解释的洪水检测系统。

## Abstract
Floods frequently impact regions around the world. Rapid and accurate flood detection is crucial for emergency response and timely mitigation of human and economic loss. The expanding availability of satellite data and advances in artificial intelligence have enhanced monitoring of environmental hazards, but many flood events remain challenging to detect because cloud cover obscures optical satellite imagery. Rambour et al. introduced the SEN12-FLOOD dataset and extracted per-image features using a ResNet-50 convolutional neural network backbone, then fed these features into a gated recurrent unit network to show that temporal information can substantially improve accuracy compared to single-image baselines. More recently, Chamatidis et al. showed that a vision transformer can achieve strong performance with popular convolutional architectures. However, these models typically function as opaque black boxes, making it difficult to interpret their decision boundaries, learned features, and internal reasoning, especially in safety-critical domains like remote sensing. In contrast, topological data analysis (TDA) provides a mathematically grounded framework for capturing global structural features of data. TDA has emerged as a powerful tool for analyzing complex imagery, especially imagery with geometrically interpretable structures, of which floods are a prime candidate. In this work, we systematically evaluate topological descriptors for flood detection using the open-source SEN12-FLOOD dataset. By extracting topological features from each image and incorporating them into neural networks, we demonstrate that topological descriptors carry meaningful flood signals independently and complement existing networks to yield more robust and interpretable flood detection systems.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：全球洪水频发，亟需快速准确的检测方法。光学卫星图像受云遮挡严重，合成孔径雷达（SAR）虽能穿透云层但存在斑点噪声。现有深度学习模型（如ResNet-50+GRU、Vision Transformer）在SEN12-FLOOD数据集上取得了不错性能，但它们作为不透明的黑箱，难以解释决策边界和内部推理，这在安全关键的遥感领域是不可接受的。
- **背景与创新**：拓扑数据分析（TDA）提供数学严谨的框架，能够捕捉数据的全局结构特征（如连通分量、空洞），且对噪声稳定、具有可解释性。本文首次系统评估TDA用于洪水检测，将拓扑描述符融入神经网络，旨在提升检测的鲁棒性和可解释性。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：从每帧图像中提取拓扑特征（通过持久同调），生成固定长度的向量表示，然后将其作为额外输入与卷积特征（ResNet-50）融合，送入门控循环单元（GRU）进行时序洪水分类。
- **关键技术细节**：
    1. **预处理**：对Sentinel-1（SAR）将VV和VH通道转为dB并合并为单通道灰度图；对Sentinel-2计算归一化差异水指数（NDWI）并取反，使水体区域强度值更低（与SAR一致）。所有图像重采样至120×120。
    2. **拓扑特征提取**：
        - 对每帧灰度图构建**子水平集立方过滤**（V-construction），计算0维（连通分量）和1维（空洞）的持久同调，得到持久图（PD）。
        - 采用**轻量高斯嵌入**（类似持久图像）：在训练集上根据出生/死亡分位数预定义G×G网格中心，用带宽σ的高斯核加权，保留每个同调维数中寿命最长的K=200个点，生成2G²维向量（本文G=10，输出200维）。该嵌入解决了原始持久图在训练中不收敛的问题。
    3. **模型结构**：
        - **Topo-GRU**：仅使用拓扑描述符作为每帧输入，GRU隐含大小H=256，输出逐帧洪水概率（带sigmoid激活）。
        - **ResNet50-GRU**：使用在BigEarthNet上预训练的ResNet-50提取2048维卷积特征，GRU头同上。对双模态数据，附加16维模态嵌入。
        - **Fusion-GRU**：将卷积特征（冻结ResNet-50）与拓扑特征拼接（共2248维），再送入GRU。双模态下为2264维。
    4. **训练**：采用类别加权逻辑损失（权重w+=负样本/正样本比），初始化分类偏置为log(π/(1-π))。Adam优化器，学习率0.001，batch size 8，最大200 epochs。模型选择基于验证集Fβ（β=√2，召回率权重为精确率的两倍），早停20轮。测试时根据Recall@90%P（召回率最大且精确率≥0.9）选择阈值τ。

### 3. 实验设计：数据集、benchmark、对比方法
- **数据集**：SEN12-FLOOD，包含335个序列（267训练，68测试），每个序列覆盖单一位点（非洲、伊朗、澳大利亚），包含多帧Sentinel-1 SAR（VV/VH）和Sentinel-2光学（10个波段）图像。其中192个序列有洪水事件。预处理后共3418张S1图像（1325张洪水）、1983张S2图像（446张洪水）。平均每序列S1:10.20帧，S2:5.92帧。
- **基准（benchmark）**：直接引用Rambour等人（2020）在相同数据集上的基线结果（ResNet-50+GRU准确率：S1 87.5%，S2 93.0%，双模态95.7%）。
- **对比方法**：
    - ResNet50-GRU（本文采用迁移学习重新实现）
    - Topo-GRU（仅拓扑特征）
    - Fusion-GRU（融合特征）
    以上均在S1、S2、双模态三种设定下进行。

### 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量及训练时长。仅提及使用PyTorch框架，Adam优化器，batch size 8，最大200 epochs，以及基于验证Fβ的早停策略。算力资源的具体信息缺失。

### 5. 实验数量与充分性
- **实验数量**：共9组主要实验（3种模型 × 3种模态），结果展示在表1（性能指标）和表2（混淆矩阵）。另外还单独报告了ResNet-50迁移学习带来的增益。未进行其他消融实验（如不同拓扑参数G、K的影响，不同TDA向量化方法的对比）。
- **充分性与公平性**：
    - 数据集划分固定，评估指标包含Fβ、F1、准确率、Recall@90%P，较全面且贴近实用（优先减少漏报）。
    - 模型选择与阈值设定均基于验证集，避免了过拟合调参的偏倚。
    - 不足：未与最新方法（如Vision Transformer）对比；未分析拓扑特征在时序变化检测中的具体物理含义；仅在单一数据集上验证，泛化性存疑；未探讨拓扑特征计算耗时或与卷积网络的互补性本质。

### 6. 论文的主要结论与发现
- **迁移学习显著提升**：使用预训练的ResNet-50在S1、S2和双模态上分别将准确率从原基线的87.5%、93.0%、95.7%提升至95.8%、98.8%、97.4%。
- **拓扑特征独立有效**：仅使用拓扑特征（Topo-GRU）在S1上达91.4%准确率，S2上达95.8%，说明拓扑描述符自身携带洪水信号，尽管弱于卷积特征。
- **融合特征最优**：Fusion-GRU在双模态下达到98.9%准确率、98.2% F1分数，优于任何单一模态或特征，证明拓扑与卷积特征互补。
- **稳定性**：所提出的高斯嵌入使拓扑特征训练收敛，优于直接使用原始持久图或标量摘要。

### 7. 优点：方法或实验设计上的亮点
- **首次将TDA用于洪水检测**：特别针对SAR图像，展示了持久同调在遥感灾害监测中的潜力。
- **轻量高斯嵌入**：解决了持久图在时序模型中难以收敛的问题，且参数（网格中心、带宽）可预先从训练集估计，稳定性好。
- **关注可解释性**：TDA特征本身具有几何意义（连通分量、空洞），有助于理解模型决策（如洪水导致水体区域在拓扑上表现为低强度连通分量扩大）。
- **实用评估指标**：采用Fβ（β=√2）和Recall@90%P，更符合实际应用中“漏报代价远高于误报”的需求。
- **数据预处理严谨**：对SAR和光学数据分别进行了标准化对数变换、NDWI计算、异常帧剔除，保证输入一致性。

### 8. 不足与局限
- **实验覆盖有限**：仅在SEN12-FLOOD一个数据集上验证，且数据集规模较小（335序列）。不同地理区域（如亚洲、美洲）未涉及，泛化性未知。
- **未与更先进基线对比**：论文只复现了Rambour的ResNet-50+GRU，未与Chatamidis等人的Vision Transformer或其他先进时序模型（如LSTM、Transformer）对比。
- **拓扑特征分析不深入**：未展示拓扑特征（如0维分量数量、空洞寿命）与实际洪水区域之间的定量关系；未与其他TDA向量化方法（持久景观、持久图像、PERSLAY）进行系统对比；未分析不同G、K取值对性能的影响。
- **计算开销未讨论**：持久同调计算（CubicalRipser）的时间成本、与卷积网络联合运行时的实时性未提及。
- **融合策略简单**：仅采用特征拼接后送入GRU，未尝试注意力机制或端到端学习拓扑嵌入（如允许网格中心可学习）。
- **资源信息缺失**：未报告算力需求，不利于复现和资源评估。

（完）
