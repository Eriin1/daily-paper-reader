Title: Multimodal Nonblocking Supervisory Control Synthesis

URL Source: https://arxiv.org/pdf/2607.03263v1

Published Time: Tue, 07 Jul 2026 00:55:45 GMT

Number of Pages: 8

Markdown Content:
Marijn Minkenberg et al. , Multimodal Nonblocking Supervisory Control Synthesis Submitted to CDC 2026 , uploaded to arXiv on July 7, 2026. 

# Multimodal Nonblocking Supervisory Control Synthesis 

M. Minkenberg 1, M. A. Reniers 1, M. A. Goorden 1, J. M. van de Mortel-Fronczak 1, W. J. Fokkink 2

Abstract — Supervisory control synthesis leverages the non-blocking property to show liveness of the supervised system. This property is particularly weak when system models include fault behavior, reconfiguration, or multiple control goals. To capture a more suitable nonblocking property for such system models, this paper introduces modal and multimodal nonblock-ing. These novel nonblocking variants impose a restriction on the states visited on the path towards a marked state. Synthesis algorithms are presented to construct modal and multimodal nonblocking supervisors. The novel nonblocking variants are illustrated with three intuitive examples, inspired by real synthesis problems encountered while applying super-visory control synthesis to safety-critical water infrastructures. A comparison is made between the novel nonblocking variants and established nonblocking variants to show that they are distinct. Additionally, where possible, conditions are formulated under which one variant implies the other. 

I. I NTRODUCTION 

Supervisory control synthesis is a method for the calcula-tion of a correct-by-construction supervisor based on a plant model [1]. It relies on nonblocking to show liveness of the su-pervised system. A supervised system is called nonblocking if it is always able to reach a marked state, often indicating the ‘rest state’ or ‘safe state’ of a system [2]. However, nonblocking relies on the assumption that the specific path that is used to reach a marked state does not matter. For plants that deal with fault behavior, are reconfigurable, or address multiple control goals, this assumption does not hold. Over the years, several nonblocking variants have been introduced to obtain more suitable nonblocking properties for certain applications. For example, generalized nonblock-ing [3] defines a subset of states from which a marked state must be reachable. This is relevant for hierarchical supervisory control. Nonblocking with progressive events [4] (henceforth progressive nonblocking) restricts the set of events that may be used to reach a marked state. This is par-ticularly useful when the plant contains rare or undesirable behavior. Multitasking nonblocking [5] introduces colored marked states, capturing that multiple rest states must always remain reachable. This is a suitable nonblocking variant for multitasking systems. Quantitative nonblocking [6] places a restriction on the number of events that may be taken to reach a marked state. This is suitable when such a metric must be limited, for example for workpiece-processing plants.              

> *This work was supported as part of STORM SAFE, an Interreg North Sea project co-funded by the European Union.
> 1Department of Mechanical Engineering, Eindhoven University of Technology, Eindhoven, the Netherlands. {m.minkenberg, m.a.reniers, m.a.goorden, j.m.v.d.mortel }@tue.nl
> 2Department of Computer Science, Vrije Universiteit Amsterdam, Ams-terdam, the Netherlands. w.j.fokkink@vu.nl

In ongoing research [7], supervisory control synthesis is being applied to safety-critical water infrastructures. These systems address various—and often conflicting—control goals, like flood protection, water discharge, fish migration, and ship traffic. Each goal typically has its own set of marked states. The active goals are reconfigured based on environmental parameters such as water levels, the time of year, and the presence of ships. For these systems, it makes sense to synthesize a super-visor that is always able to reach a marked state without changing the active set of control goals. After all, the system has little to no influence over when or whether the control goals change. In addition, for critical infrastructures, it is essential to include error states and have the supervisor deal with fault behavior. Naturally, the supervisor should not rely on error states to reach a marked state. Synthesizing such a supervisor requires a nonblocking variant that restricts the states visited on the path to a marked state. This precise control problem is not addressed by established nonblocking variants. Multitasking nonblocking captures the reachability of multiple marked states at once, but it does not restrict how any of those marked states are reached. Progressive nonblocking does place restrictions on the path to a marked state. However, the restriction is expressed in events and can only be applied for a single configuration or control goal at a time. In general, it is desirable to be able to express nonblocking without relying on fault behavior, reconfiguration, or differ-ent control goals. It is an easy enough modeling task to define a set of states belonging to a certain configuration, or a set of states without a system error. However, there is no method to synthesize a supervisor that guarantees nonblocking without ever leaving such a state set. This paper introduces modal nonblocking and multimodal nonblocking as novel nonblocking variants. They enable the modeler to specify one or more state sets and show that a marked state can be reached without leaving the set(s). These variants provide a more expressive and versatile nonblocking property that can be used for the aforementioned control problem with safety-critical water infrastructures. Additionally, algorithms are presented for synthesizing modal and multimodal nonblocking supervisors. The com-putational complexity of the algorithms scales linearly with the number of defined state sets. The synthesized supervisors are shown to be controllable and maximally permissive. Finally, the novel nonblocking variants are compared to previously established ones to show that they are distinct. Where possible, specific conditions are given under which one nonblocking variant implies the other. 

> arXiv:2607.03263v1 [eess.SY] 3 Jul 2026

II. P RELIMINARIES 

A. Finite automata 

This paper uses finite automata , as introduced in [8]. 

Definition 1 (Finite Automaton): A finite automaton (FA) is a 5-tuple P = ( Q, Σ, , Q m, q 0), where:  

> •

Q is a finite set of states;  

> •

Σ is a finite set of events, partitioned into controllable events Σc and uncontrollable events Σu; 

> •

⊆ Q × Σ × Q is the set of transitions;  

> •

Qm ⊆ Q is the set of marked states;  

> •

q0 ∈ Q is the initial state. An FA P1 = ( Q1, Σ, 1, Q m, 1, q 0) is a subautomaton 

of FA P2 = ( Q2, Σ, 2, Q m, 2, q 0), denoted P1 ⊑ P2, iff 

Q1 ⊆ Q2, 1 ⊆ 2, and Qm, 1 = Qm, 2 ∩ Q1. In other words, P1 ⊑ P2 if P1 can be obtained from P2 by removing transitions and/or states, where all transitions that originate from or go to removed states are also removed [9]. A transition (q, σ, q ′) ∈ , also written as q σ q′,defines that P is able to go from state q to state q′ by exe-cuting event σ. The automata in this paper are deterministic ,meaning that q σ q′ and q σ q′′ always implies q′ = q′′ .The following notations are used:  

> •

q q′ denotes that q σ q′ for some σ ∈ Σ; 

> •

q q′ denotes that q σu

q′ for some σu ∈ Σu; 

> •

q −1 q′ denotes that q′ q; 

> •

q q′ denotes that q q′′ and q′′ q′ for some 

q′′ , or q = q′, and this applies to and x in the same way. 

Definition 2 (Reachable): A state q ∈ Q is considered 

reachable , denoted R(q), iff q0 q. An FA P = ( Q, 

Σ, , Q m, q 0) is reachable iff ∀q ∈ Q(R(q)) .

B. Supervisory control synthesis 

Supervisory control synthesis [1] constructs a supervisor FA S based on a plant FA P . In this paper, it is assumed that 

S ⊑ P and S is reachable. The supervisor interacts with the plant by disabling some controllable events. The synthesis process must construct a supervisor that is nonblocking ,

controllable , and maximally permissive .The nonblocking property of a supervisor depends on the nonblocking properties of its states. 

Definition 3 (State properties): Let P = (Q, Σ, ,Qm, q 0) be an FA. A state q ∈ Q is:  

> •

nonblocking , denoted N(q), iff ∃qm ∈ Qm (q qm); 

> •

blocking iff ¬N(q); 

> •

bad , denoted B(q), iff ∃q′ ∈ Q (¬N(q′) ∧ q q′).A supervisor is nonblocking if all reachable states in the supervisor are nonblocking states. 

Definition 4 (Standard nonblocking [2]): Let P = ( Q, 

Σ, , Q m, q 0) be an FA. P is standard nonblocking or simply nonblocking , denoted N(P ), iff ∀q ∈ Q (R(q) ⇒

N(q)) .A supervisor is controllable for a plant if it never disables uncontrollable events in Σu that are possible in the plant. 

Definition 5 (Controllable [10]): Let P = ( Q, Σ, ,Qm, q 0) be a plant FA, let S = ( QS , ΣS , S , Q m,S ,q0,S ) be a supervisor FA, and let Σu ⊆ Σ be the set of uncontrollable events. S is controllable for P iff ∩(QS ×

Σu × Q) ⊆ S .A supervisor that is nonblocking and controllable is called 

proper . Finally, a supervisor is maximally permissive if it does not disable more behavior than necessary to be proper. 

Definition 6 (Maximally permissive): Let P be a plant FA and S be a proper supervisor FA. S is maximally permissive 

iff for each proper supervisor FA S′ it holds that S′ ⊑ S.

C. Nonblocking property 

To obtain the set of nonblocking states, a B ACKWARD 

REACHABILITY SEARCH is defined, see Algorithm 1. Its format and use in synthesis are inspired by [10]. 

Algorithm 1 BACKWARD REACHABILITY SEARCH (BRS ). 

Input: Considered state set Qc, starting state set Qs,transition relation 

Output: Maximal state set Qr ⊆ Qc from which a state 

q ∈ Qc ∩ Qs is reachable with transitions in  

> 1:

i ← 0, Q i ← Qc ∩ Qs 

> 2:

repeat  

> 3:

Qi+1 ← Qi ∪ { q ∈ Qc|q q′, q ′ ∈ Qi} 

> 4:

i ← i + 1  

> 5:

until Qi = Qi−1 

> 6:

Qr ← Qi

This algorithm takes all states in the set Qc ∩ Qs, and iteratively finds more states in Qc that have a sequence of transitions in to a state in this set. The loop terminates when no new states are found. At that point, Qi contains all states that can reach Qc ∩ Qs with transitions in .

BRS can be used to find the set of nonblocking states, the set of bad states, and the set of reachable states. This is used for synthesis, as presented in Algorithms 2 and 3. 

Lemma 1: [10]. Let P = ( Q, Σ, , Q m, q 0) be an FA, and let Qblocking = {q ∈ Q|¬ N(q)} be the set of blocking states of P . Then, the following holds:  

> •

∀q ∈ Q (N(q) ⇔ q ∈ BRS( Q, Q m, )) ; 

> •

∀q ∈ Q (B(q) ⇔ q ∈ BRS( Q, Q blocking , )) ; 

> •

∀q ∈ Q (R(q) ⇔ q ∈ BRS( Q, {q0}, −1)) .III. M OTIVATING EXAMPLES 

This section features three examples to illustrate different weaknesses of nonblocking. Each example is also encoun-tered when applying supervisory control synthesis to safety-critical water infrastructures. For each example, it is shown how a more suitable nonblocking property can be obtained. 

A. Example 1: System with error states 

Consider an FA that includes error states, such as the one shown in Fig. 1. Here, states C and D represent error states. The FA is nonblocking, but this relies on states C and D to be visited. It is argued that a stronger nonblocking property is necessary that determines nonblocking without relying on these error states. 2A B 

> CD
> E
> σ1
> σ2
> σerror
> σ1
> σ2
> σfix

Fig. 1: Example FA for a system with error states. Suppose that a subset of states is chosen to describe an error-free mode. Here, this is the set {A, B, E }, as highlighted in blue. It is then clear that, from states A and B,there exists no path towards a marked state without leaving this set. In other words, the system becomes blocking if it is restricted to the error-free mode. By defining the error-free mode, it becomes possible to express that this FA is only nonblocking when it is allowed to leave the error-free mode. The modeler might want to reconsider whether this behavior is desirable. 

B. Example 2: System with reconfiguration 

The study in [11] introduces a framework for modeling product lines with reconfigurable features. Their running example is a coffee machine where various features may 

come or go at any moment, for example as a result of maintenance. Although the work shows that a nonblocking supervisor can be synthesized for the overall system, it has not been considered whether nonblocking holds for each individual configuration. Because the system may operate for a long time in one configuration, it is desirable that each configuration is nonblocking when considered in isolation. A small FA with reconfiguration is shown in Fig. 2. Here, reconfiguration events σX come and σX go represent some feature X coming or going from the system. In this example, states B1 and B2 represent a mode where feature X is present in the system. Because neither of these two states is marked, the system is never in a marked state while X

is present, which may be indefinitely. However, the FA is nonblocking because marked state P2 is always reachable. The nonblocking property relies on ever returning to states where feature X is not present. One way to reveal this issue is to consider nonblocking separately for each configuration. In the example, the con-figurations are given by state sets {P1, P 2} and {B1, B 2},highlighted in blue and orange, respectively. Both states in  

> P1P2
> B1B2
> σ1
> σ2
> σX come
> σX go
> σ3
> σ4
> σX come
> σX go

Fig. 2: Example FA for a system with reconfiguration. set {P1, P 2} can reach a marked state while staying in the set. However, neither state in set {B1, B 2} can reach a marked state while staying in the set. As a result, B1 and 

B2 are blocking states within this set—a clear indication that there is an issue in this FA, whenever X is present. 

C. Example 3: System with multiple control goals 

Consider an FA that describes two controlled traffic lights at a crossing, shown in Fig. 3. The state names indicate for each traffic light whether they are red ( R) or green ( G). The FA leaves out the GG state, where both traffic lights are green simultaneously. Clearly, this FA is nonblocking, which guarantees that at least one traffic light can turn green. However, nonblocking alone does not guarantee that each traffic light can turn green when their behavior is isolated.  

> RR GR RG
> σgreen2
> σred2 σgreen1
> σred1

Fig. 3: Example FA for a system with multiple control goals. Suppose a subset of states is chosen that isolates the behavior of the first traffic light (the set {RR, GR }, as highlighted in blue). If the FA is nonblocking within this set, it is known more specifically that the first traffic light can turn green without relying on the other traffic light. The same is possible separately for the second traffic light by choosing the set {RR, RG }, as highlighted in orange. IV. M ODAL NONBLOCKING 

In each of the previous examples, a more suitable non-blocking property is obtained by restricting the set of states that may be visited on the path to a marked state. This section formalizes that method by introducing the novel concept of modal nonblocking. 

A. Definition of modal nonblocking 

Modal nonblocking is based on a parameter M ⊆ Q,which is a subset of plant states, referred to as a mode. A mode may represent error-free states, states of a system configuration, or states related to a specific control goal. For a state q ∈ M , modal nonblocking requires that it is possible to reach a marked state without leaving M . In other words, any state q̸ ∈ M should not be relied upon to reach a marked state. A state q̸ ∈ M itself is defined as modal nonblocking by default. 

Definition 7 (Modal state properties): Let P = ( Q, Σ,, Q m, q 0) be an FA, let M ⊆ Q be a state set of interest, and let M = ∩ M × Σ × M be the set of transitions 

within M . A state q ∈ Q is:  

> •

modal nonblocking for M , denoted MN M (q), iff q ∈

M ⇒ ∃ qm ∈ Qm (q M qm); 

> •

modal blocking for M iff ¬MN M (q); 

> •

bad , denoted B(q), iff ∃q′ ∈ Q (¬MN M (q′)∧q q′).3Note that M is introduced as the set of transitions within M . If there exists a sequence of transitions within 

M , then that sequence also exists in general. 

Lemma 2: Let P = ( Q, Σ, , Q m, q 0), let q and q′ be states in Q, let M ⊆ Q be a state set of interest, and let  

> M

= ∩ M × Σ × M . Then, q M q′ ⇒ q q′.

Proof: Suppose there exists a sequence of transitions from q to q′ in M . Every transition in this sequence must also be in , because by the construction of M it holds that M ⊆ . Therefore, the whole sequence of transitions is also in . Thus, q M q′ ⇒ q q′.Similar to finding the set of nonblocking, bad, and reach-able states (see Lemma 1), the set of modal nonblocking states in M can be found using BRS .

Lemma 3: Let P = (Q, Σ, , Q m, q 0) be an FA, and let M ⊆ Q be a state set of interest. Then ∀q ∈

M (MN M (q) ⇔ q ∈ BRS( M , Q m, )) .

Proof: This proof is structured over the lines in BRS 

where Qi is constructed, which becomes the output Qr on line 6. 1: Q0 is constructed as M ∩ Qm. Thus, Q0 is the set of states in M that can reach a state in M ∩ Qm using 0 transitions in , while staying in M .3: Qi+1 is constructed as Qi extended with the set of states in M that can reach a state in Qi after 1 transition in . Since Qi ⊆ M , both q and q′ are in M . Thus, 

Qi+1 is the set of states in M that can reach a state in 

M ∩ Qm using i + 1 or less transitions in , while staying in M .5: The loop finishes when Qi = Qi−1. Because Qi−1 ⊆

Qi, the set of states Qi only grows. Since Qi ⊆ M and 

M is finite, the expansion of Qi must be finite. As a result, Qr is the set of all states in M that can reach a state in M ∩ Qm using any number of transitions in , while staying in M .At every step of its construction, it holds that MN M (q) for all states in Qi. The output of BRS , Qr , is the set of all states in M for which MN M (q) holds. Thus, for q ∈ M , it is proven that q ∈ BRS( M , Q m, ) ⇔ MN M (q).The modal nonblocking property for an FA requires that all its reachable states are modal nonblocking. It does not pose additional restrictions on the reachability of a state. 

Definition 8 (Modal nonblocking FA): Let P = ( Q, Σ,, Q m, q 0) be an FA, and let M ⊆ Q be a state set of interest. P is modal nonblocking for M , denoted MN M (P ),iff ∀q ∈ Q (R(q) ⇒ MN M (q)) .The definition is illustrated using Example 1, where M =

{A, B, E }. All states in the FA are reachable. States C and 

D are not in M , and therefore they are modal nonblocking by default. State E is modal nonblocking, since it is marked. States A and B are not modal nonblocking: both states are in 

M and cannot reach a marked state without visiting a state outside of M . Because not all reachable states are modal nonblocking, the FA is also not modal nonblocking. 

B. Modal nonblocking synthesis 

The goal of synthesis with modal nonblocking is to produce a supervisor that is modal nonblocking, controllable, and maximally permissive. Algorithm 2 presents a synthesis algorithm for this purpose, relying on the BRS function from Algorithm 1 to keep the presentation compact. Algorithm 2 is a small adjustment of the standard synthesis algorithm used in [10], where the changes are highlighted in blue. Note that this algorithm is not optimized for ef-ficiency; however, the computational complexity of modal nonblocking is equal to that of synthesis with the standard nonblocking property. 

Algorithm 2 Synthesis with modal nonblocking. 

Input: plant (Q, Σ, , Q m, q 0) and M ⊆ Q

Output: supervisor (QS , Σ, ∩ (QS × Σ × QS ),Qm ∩ QS , q 0 ∩ QS ) 

> 1:

k ← 0, Q k ← Q 

> 2:

repeat  

> 3:

QkM ← Qk ∩ M 

> 4:

N k ← BRS( QkM , Q m, ) 

> 5:

Bk ← BRS( Qk, Q kM \ N k, ) 

> 6:

Qk+1 ← Qk \ Bk 

> 7:

k ← k + 1  

> 8:

until Qk = Qk−1 

> 9:

QS ← BRS (Qk, {q0}, −1)The newly introduced input set M is used on line 3 to specify the set of states QkM for which modal nonblocking should be determined. States outside of M are always modal nonblocking and are therefore excluded from this set. The set of modal nonblocking states is obtained on line 4 using Lemma 3. The set of modal blocking states is obtained by subtracting the set of modal nonblocking states N k from 

QkM , because only states in QkM can become modal blocking. The set of bad states Bk is obtained on line 5, using the second item of Lemma 1. Here, the considered state set is 

Qk, because any state in Qk can become a bad state: this must not be limited to only states in QkM .After the computation of bad states Bk, they are removed from Qk on line 6. Thus, the algorithm iteratively refines the set Qk, each time taking out any blocking states, until Qk

is the same in two consecutive iterations. Finally, BRS is used one more time on line 9 to obtain the set of reachable states, using the third item of Lemma 1. Because at the end all states in Qk are modal nonblocking, it also holds that all reachable states are modal nonblocking. Thus, a modal nonblocking FA is constructed. In fact, Algorithm 2 constructs a supervisor that is modal nonblocking for M , controllable, and maximally permissive. However, instead of proving the correctness of Algorithm 2 directly, the full proof is given in Theorem 1 for synthesis with multimodal nonblocking, of which synthesis with modal nonblocking is a special case. 4V. M ULTIMODAL NONBLOCKING 

Examples 2 and 3 have shown the use of modal non-blocking with respect to multiple state sets of interest. This motivates the novel notion of multimodal nonblocking, which is introduced and defined in this section. 

A. Definition of multimodal nonblocking 

Multimodal nonblocking is defined for multiple state sets of interest, collected in the set M, where M ⊆ 2Q.

Definition 9 (Multimodal state properties): Let P = ( Q, 

Σ, , Q m, q 0) be an FA, and let M ⊆ 2Q. A state q ∈ Q

is:  

> •

multimodal nonblocking for M, denoted MMN M(q),iff ∀M ∈ M (MN M (q)) ; 

> •

multimodal blocking for M iff ¬MMN M(q); 

> •

bad , denoted B(q), iff ∃q′ ∈ Q (¬MMN M(q′) ∧ qq′).This definition leverages the modal nonblocking definition from Definition 7, adding that it must hold for every state set of interest in M. Note that a state that is not contained in any state sets of interest is always multimodal nonblocking. In addition, a marked state is always multimodal nonblocking. 

Lemma 4: Let P = ( Q, Σ, , Q m, q 0) and let M ⊆ 

2Q. Then, ∀q ∈ Qm (MMN M(q)) .

Proof: For all q ∈ Qm and M ∈ M , it is certain that MN M (q), since q ∈ M ⇒ ∃ qm ∈ Qm (q M qm) is already true by not taking any transitions. It thus holds that 

MMN M(q) for all q ∈ Qm.Multimodal nonblocking is suitable for Example 3, where 

M = {M b, M o} (as indicated by the blue and orange colored states in Figure 3, respectively). State RR belongs to both M b and M o, so MMN M(RR ) iff MN Mb (RR ) ∧

MN Mo (RR ). Observe that indeed RR is able to reach marked state GR while staying within M b, and RR is also able to reach marked state RG while staying within M o.Thus, MMN M(RR ) holds. By Lemma 4, both marked states GR and RG are multimodal nonblocking. When 

MMN M(q) holds for all reachable states q of an FA P ,the FA itself is multimodal nonblocking. 

Definition 10 (Multimodal nonblocking FA): Let P =(Q, Σ, , Q m, q 0) be an FA, and let M ⊆ 2Q. P is 

multimodal nonblocking for M, denoted MMN M(P ), iff 

∀q ∈ Q (R(q) ⇒ MMN M(q)) .This can now be used to show that Example 2 is not multi-modal nonblocking for M = {M b, M o} (as indicated by the blue and orange colored states in Figure 2, respectively). All states are reachable, and all states are modal nonblocking for 

Mb. However, states B1 and B2 are not modal nonblocking for Mo, since there is no marked state that can be reached while staying within Mo. Therefore, the FA is clearly not multimodal nonblocking for M.

B. Multimodal nonblocking synthesis 

The algorithm for synthesis with multimodal nonblocking is presented in Algorithm 3, where the changes with respect to Algorithm 2 are highlighted in blue. Note that the compu-tational complexity of Algorithm 3 scales linearly with |M| .

Algorithm 3 Synthesis with multimodal nonblocking. 

Input: plant (Q, Σ, , Q m, q 0) and M ⊆ 2Q

Output: supervisor (QS , Σ, ∩ (QS × Σ × QS ),Qm ∩ QS , q 0 ∩ QS ) 

> 1:

k ← 0, Q k ← Q 

> 2:

repeat  

> 3:

for all M ∈ M do  

> 4:

QkM ← Qk ∩ M 

> 5:

N kM ← BRS( QkM , Q m, ) 

> 6:

end for  

> 7:

Bk ← BRS( Qk, S 

> M∈M

[QkM \ N kM ], ) 

> 8:

Qk+1 ← Qk \ Bk 

> 9:

k ← k + 1  

> 10:

until Qk = Qk−1 

> 11:

QS ← BRS( Qk, {q0}, −1)

This algorithm iterates over the state sets M defined in M. Each iteration constructs N kM , describing the set of nonblocking states for each M . On line 7, the term S 

> M∈M

[QkM \ N kM ] combines the blocking states for each 

M , forming Qblocking . After that, the synthesis algorithm continues in the same way as Algorithm 2. Algorithm 3 also performs synthesis with modal nonblock-ing or standard synthesis for specific inputs:  

> •

For M = {M }, synthesis with modal nonblocking for 

M is performed, because lines 3 to 6 are executed once for M . 

> •

For M = {Q}, standard synthesis is performed, be-cause lines 3 to 6 are executed once for M = Q.For any plant model and M, Algorithm 3 constructs a su-pervisor that is multimodal nonblocking for M. Additionally, the controllability and maximal permissiveness is ensured. 

Theorem 1: Let P = ( Q, Σ, , Q m, q 0) be an FA with a set of uncontrollable events Σu ⊆ Σ, and let M ⊆

2Q. Algorithm 3 constructs a supervisor S = ( QS , ΣS , 

> S

, Q m,S , q 0,S ) that is multimodal nonblocking for M,controllable for P , and maximally permissive. 

Proof: Multimodal nonblocking for M: Starting from Definition 10, it must be proven that ∀q ∈ QS (R(q) ⇒

MMN M(q)) . Here, MMN M(q) can be replaced with Definitions 7 and 9 to get: ∀q ∈ QS (R(q) ⇒ ∀ M ∈ M (q ∈

M ⇒ ∃ qm ∈ Qm (q M qm))) . Let q ∈ QS and assume 

R(q). Then, let M ∈ M and assume q ∈ M . It remains to be proven that ∃qm ∈ Qm (q M qm).On line 11, QS is constructed based on BRS . A property of BRS is that Qr ⊆ Qc. Therefore, QS ⊆ Qk, and since 

q ∈ QS , also q ∈ Qk. Suppose the loop terminates when 

K = k − 1, so when QK+1 = QK , following line 10. From line 8, it can be concluded that BK = ∅, applying QK+1 =

QK and knowing that neither set is empty (since q ∈ QK+1 ). 

BK is the output of BRS on line 7. This output is at least 

Qc ∩ Qs, so it is the empty set iff Qc ∩ Qs is also the empty set. So, taking the BRS inputs on line 7, it must hold that 

QK ∩ (S 

> M∈M

[QKM \ N KM ]) = ∅.From line 4, it is known that QKM ⊆ QK for every M .5Then, also QKM \N KM ⊆ QK for every M . Apply that A∩B =

A when A ⊆ B to obtain S 

> M∈M

[QKM \ N KM ] = ∅. Thus, 

QKM ⊆ N KM for every M .Combining that q ∈ M as well as q ∈ QK , by line 4 also q ∈ QKM . Since QKM ⊆ N KM for every M , it is known that q ∈ N KM for every M . N KM is the output state set of 

BRS( QKM , Q m, ) on line 5. Because QKM ⊆ M by line 4, it also holds that q ∈ BRS( M , Q m, ). By Lemma 3, this means MN M (q). Thus, by Definition 7, ∃qm ∈ Qm (q M

qm), which was required for the proof. 

Controllable: Start from Definition 5: ∩ (QS × Σu ×

Q) ⊆ S . This holds if (qS , σ u, q ) ∈ ⇒ (qS , σ u, q ) ∈ 

> S

, where qS ∈ QS , σu ∈ Σu, and q ∈ Q. Assume 

(qS , σ u, q ) ∈ , and prove (qS , σ u, q ) ∈ S .Since qS ∈ QS , also qS ∈ Qk by line 11. Suppose 

qS ∈ Bk for some k. Then qS̸ ∈ Qk by line 8, which is a contradiction. Therefore, qS̸ ∈ Bk for any k. This means that ̸∃ q′ ∈ Q (¬MMN M(q′) ∧ qS q′): for any multimodal blocking state q′ ∈ Q, there is no sequence of uncontrollable events from qS to q′. In other words, for all qS ∈ QS any sequence of uncontrollable events leads to a state that is multimodal nonblocking. That state, consequently, is also in 

QS . Since S = ∩ (QS × Σ × QS ), it is certain that 

(qS , σ u, q ) ∈ S .

Maximally permissive: Proof is done by contradiction. Suppose there is a proper supervisor FA S′ = ( Q′ 

> S

, ΣS , 

> ′
> S

, Q ′ 

> m,S

, q 0,S ) ⊑ P such that S′̸ ⊑ S. By Definition 6, this means that either Q′

> S

̸ ⊆ QS , or ′

> S

̸ ⊆ S , or 

Q′

> m,S

̸ = Qm,S ∩ Q′ 

> S

. 

> •

Q′

> S

̸ ⊆ QS : Assume ∃q′ (q′ ∈ Q′ 

> S

∧ q′̸ ∈ QS ). There are two lines in Algorithm 3 where states are potentially removed from Q to obtain QS : line 8 and line 11. Suppose, on line 11, q′̸ ∈ BRS( Qk, {q0}, −1).Then, by the third item of Lemma 1, ¬R(q′). Sup-pose, on line 8, q′ ∈ Bk for some k. Then, q′ ∈

BRS( Qk, Q blocking , ). By Lemma 1, this means that B(q′). Thus, for all states q′̸ ∈ QS , it holds that B(q′) ∨ R(q′). If q′ ∈ Q′ 

> S

, this contradicts the assumption that S′ is reachable and proper. Therefore, if q′̸ ∈ QS , it cannot be that q′ ∈ Q′ 

> S

, because S′ would not be proper. Thus, Q′ 

> S

⊆ QS . 

> •′
> S

̸ ⊆ S : Assume ∃t′ (t′ ∈ ′ 

> S

∧ t′̸ ∈ S ),where t′ = ( q′

> S, 1

, σ, q ′

> S, 2

) and q′

> S, 1

, q ′ 

> S, 2

∈ Q′ 

> S

. Transi-tion t′ could only have been removed from S when 

q′

> S, 1

̸ ∈ QS or q′

> S, 2

̸ ∈ QS . For every q̸ ∈ QS , it has already been shown that either B(q) or ¬R(q). Since 

S′ is proper, then also q′

> S, 1

, q ′

> S, 2

̸ ∈ Q′ 

> S

. Thus, it cannot be that t′ ∈ ′ 

> S

, and therefore ′ 

> S

⊆ S . 

> •

Q′

> m,S

̸ = Qm,S ∩ Q′ 

> S

: Assume ∃q′ 

> m

(q′ 

> m

∈ Q′ 

> m,S

∧ q′

> m

̸ ∈

Qm,S ∩ Q′ 

> S

). Given that Q′ 

> S

⊆ QS , q′ 

> m

∈ Q′ 

> m,S

,and Q′ 

> m,S

⊆ Q′ 

> S

, it holds that q′ 

> m

∈ Q′ 

> S

. Then 

Q′

> m,S

̸ = Qm,S ∩ Q′ 

> S

only if q′

> m

̸ ∈ Qm,S . However, in Algorithm 3, the marking of states is not adjusted. Thus, Q′ 

> m,S

= Qm,S ∩ Q′ 

> S

.It has been shown that Q′ 

> S

⊆ QS , ′ 

> S

⊆ S , and 

Q′ 

> m,S

⊆ Qm,S for a proper supervisor FA S′. Therefore, for each proper supervisor FA S′ it holds that S′ ⊑ S.VI. R ELATED NONBLOCKING VARIANTS 

This section briefly recalls other nonblocking variants and compares them to modal and multimodal nonblocking. It is shown that modal and multimodal nonblocking are unique nonblocking variants. In addition, conditions are provided under which one nonblocking variant implies the other. 

A. Standard nonblocking 

Neither standard nonblocking (see Definition 4) nor modal nonblocking implies the other. 

Proposition 1: 

1) There exists an FA P and an M ⊆ Q such that 

MN M (P )̸ ⇒ N(P ).2) There exists an FA P and an M ⊆ Q such that 

N(P )̸ ⇒ MN M (P ).

Proof: 1) is shown in Fig. 4a, and 2) is shown in Fig. 4b. 

A B

σ

(a) M = {A} and Qm = {A}.

C D

σ

(b) M = {C} and Qm = {D}.

Fig. 4: Example FAs. When M = {M }, the same examples can be used to show that neither standard nonblocking nor multimodal nonblocking implies the other. While there is no general implication between standard nonblocking and modal nonblocking, there does exist an implication relation for the special case where M = Q.

Corollary 1: Let P = ( Q, Σ, , Q m, q 0) be an FA. Then, MN Q(P ) ⇔ N(P ).

Proof: Definitions 4 and 8 coincide when M = Q.The same holds for multimodal nonblocking where M =

{Q}. In addition, the case where M covers Q is considered. 

Proposition 2: Let P = ( Q, Σ, , Q m, q 0) be an FA, and let M ⊆ 2Q, where S 

> M∈M

(M ) = Q. Then, 

MMN M(P ) ⇒ N(P ).

Proof: Because S 

> M∈M

(M ) = Q, for every q ∈ Q also 

q ∈ M for some M . Given that P is modal nonblocking for every M ∈ M , for every reachable state in M there must exist a path to a marked state that stays within M . By Lemma 2, there generally exists a path to a marked state, and therefore P must be standard nonblocking. The opposite implication does not hold. This is shown by the FA in Fig. 4b when M = {{ C}, {D}} . For this P , it holds that N(P ), but not MMN M(P ).

B. Generalized nonblocking 

For generalized nonblocking, a subset of states Q′ ⊆ Q is defined from which a marked state should be reachable. 

Definition 11 (Generalized nonblocking FA [3]): Let 

P = ( Q, Σ, , Q m, q 0) be an FA, and let Q′ ⊆ Q be a state set. P is generalized nonblocking for Q′, denoted 

GN Q′ (P ), iff ∀q ∈ Q′ (R(q) ⇒ N(q)) .Neither generalized nonblocking nor modal nonblocking implies the other. 6Proposition 3: 

1) There exists an FA P , an M ⊆ Q and a Q′ ⊆ Q such that MN M (P )̸ ⇒ GN Q′ (P ).2) There exists an FA P , an M ⊆ Q and a Q′ ⊆ Q such that GN Q′ (P )̸ ⇒ MN M (P ).

Proof: Standard nonblocking is a special case of generalized nonblocking where Q′ = Q. Therefore, the examples from Fig. 4 show 1) and 2) for Q′ = Q.While there is no general implication between generalized nonblocking and modal nonblocking, there is an implication relation for the special case where Q′ = M .

Proposition 4: Let P = ( Q, Σ, , Q m, q 0) be an FA, and let Q′ ⊂ Q. Then, MN Q′ (P ) ⇒ GN Q′ (P ).

Proof: It must be proven that GN Q′ (P ), or by Defi-nition 11: ∀q ∈ Q′ (R(q) ⇒ N(q)) . Assume MN Q′ (P ), so by Definitions 7 and 8: ∀q ∈ Q (R(q) ⇒ (q ∈ Q′ ⇒ ∃ qm ∈

Qm (q Q′ qm))) . Thus, for all reachable states q ∈ Q′ it holds that ∃qm ∈ Qm (q Q′ qm). By Lemma 2, then also 

∃qm ∈ Qm (q qm), denoted N(q). This means N(q) for all reachable states in Q′, which was to be proven. The opposite implication does not hold. This is shown by the FA in Fig. 1 when Q′ = {A, B, E }. For this P , it holds that GN Q′ (P ), but not MN Q′ (P ).

C. Progressive nonblocking 

For progressive nonblocking, a set of progressive events 

Σp ⊆ Σ is defined. Only progressive events may be used to reach a marked state. 

Definition 12 (Progressive nonblocking FA [4]): Let 

P = ( Q, Σ, , Q m, q 0) be an FA, let Σp ⊆ Σ be a set of progressive events, and let p = ∩ Q × Σp × Q

be the set of transitions labeled with progressive events. P

is progressive nonblocking for Σp, denoted PN Σp (P ), iff 

∀q ∈ Q (R(q) ⇒ ∃ qm ∈ Qm (q p qm)) .Neither progressive nonblocking nor modal nonblocking implies the other. 

Proposition 5: 

1) There exists an FA P , an M ⊆ Q and a Σp ⊆ Σ such that MN M (P )̸ ⇒ PN Σp (P ).2) There exists an FA P , an M ⊆ Q and a Σp ⊆ Σ such that PN Σp (P )̸ ⇒ MN M (P ).

Proof: Standard nonblocking is a special case of progressive nonblocking where Σp = Σ. Therefore, the examples from Fig. 4 show 1) and 2) for Σp = Σ .While there is no general implication between progressive nonblocking and modal nonblocking, there is an implication under a specific condition. 

Proposition 6: For an FA P , mode M ⊆ Q and Σp ⊆ Σ

such that ∀(q, σ, q ′) ∈ (q ∈ M ⇒ (q′ ∈ M ⇔ σ ∈

Σp)) , it holds that MN M (P ) ⇒ PN Σp (P ).

Proof: The construction of Σp is such that for any transition (q, σ, q ′) ∈ where q ∈ M , it always holds that q′ ∈ M ⇔ σ ∈ Σp: 

> •

when q′ also in M , then σ ∈ Σp; 

> •

when q′ not in M , then σ̸ ∈ Σp.Thus, all transitions leaving M are not in Σp, and all other transitions are in Σp. Then, MN M (P ) ⇒ PN Σp (P ).The opposite implication does not hold, which is shown by the FA in Fig. 4a when Σp = ∅. The FA is modal nonblocking and the transitions leaving M are not in Σp, yet from state B no marked state can be reached, and therefore the FA is not progressive nonblocking. Using Proposition 6 on Example 1, it can be shown that progressive nonblocking with Σp = {σ1, σ 2, σ fix } is equal to modal nonblocking with M = {A, B, E }. Indeed, by leaving the event σerror out of the set of progressive events, progressive nonblocking effectively requires that M may not be left to reach a marked state. However, progressive nonblocking cannot be applied to Examples 2 and 3 because they feature multiple state sets of interest. This would require multiple sets of progressive events to be defined, which is not considered in [4]. 

D. Multitasking nonblocking 

For multitasking nonblocking, sets of colored marked states are defined. A state is considered multitasking non-blocking iff, for each color, there exists a path towards a marked state of that color. 

Definition 13 (Multitasking nonblocking FA [5]): 

Let P = (Q, Σ, , Q m, q 0) be an FA, and let 

C ⊆ 2Q contain sets of colored marked states C. P

is multitasking nonblocking for C, denoted MTN C (P ), iff 

∀q ∈ Q (R(q) ⇒ ∀ C ∈ C (∃q′ ∈ C (q q′))) .Neither multitasking nonblocking nor modal nonblocking implies the other. 

Proposition 7: 

1) There exists an FA P , an M ⊆ Q and a C ⊆ 2Q such that MN M (P )̸ ⇒ MTN C (P ).2) There exists an FA P , an M ⊆ Q and a C ⊆ 2Q such that MTN C (P )̸ ⇒ MN M (P ).

Proof: Standard nonblocking is a special case of multitasking nonblocking, where C = {Qm}. Therefore, the examples from Fig. 4 show 1) and 2) for C = {Qm}.There are, in fact, no non-trivial conditions where there is an implication between multitasking nonblocking and modal, or multimodal, nonblocking. In other words, the variants are complimentary. Multitasking nonblocking guarantees that multiple colored marked states in different modes remain reachable, while multimodal nonblocking guarantees for each mode that a marked state can be reached while staying within that mode. Their complimentary nature can be shown in Examples 2 and 3. In Example 2, one may choose C = {{ A2}, {B1}} 

to guarantee that a colored marked state in both modes can always be reached. In addition, multimodal nonblocking guarantees that each mode itself is nonblocking without leaving that mode. In Example 3, one may choose C =

{{ GR }, {RG }} , to guarantee that the green state of either traffic light is always reachable. In addition, multimodal nonblocking guarantees that both traffic lights are able to turn green when considered separately, i.e., one light can turn green without depending on the other turning green. 7E. Quantitative nonblocking 

For quantitative nonblocking, a positive integer k is de-fined. A state is quantitative nonblocking if there exists a sequence of transitions towards a marked state consisting of 

k or less transitions. 

Definition 14 (Quantitative nonblocking FA [6]): Let 

P = (Q, Σ, , Q m, q 0), and let q k q′ denote that q q′ in at most k ∈ N0 transitions. P is 

quantitative nonblocking for k, denoted QN k(P ), iff 

∀q ∈ Q (R(q) ⇒ ∃ qm ∈ Qm (q k qm)) .Neither quantitative nonblocking nor multitasking non-blocking implies the other. 

Proposition 8: 

1) There exists an FA P , an M ⊆ Q and a k ∈ N0 such that MN M (P )̸ ⇒ QN k(P ).2) There exists an FA P , an M ⊆ Q and a k ∈ N0 such that QN k(P )̸ ⇒ MN M (P ).

Proof: Standard nonblocking is a special case of quantitative nonblocking where k = |Q|. Therefore, the examples from Fig. 4 show 1) and 2) for k = |Q|.While there is no general implication between quantitative nonblocking and modal nonblocking, there is an implication for multimodal nonblocking under a specific condition. 

Proposition 9: For an FA P , integer k and sets of desired states M ⊆ 2Q, constructed such that each M contains states reachable in k or less transitions from a state q: M (q) = 

{q′ ∈ Q|q k q′} and M = {M (q)|q ∈ Q}, it holds that 

MMN M(P ) ⇒ QN k(P ).

Proof: Replace both sides with Definitions 10 and 14: 

∀q ∈ Q (R(q) ⇒ MMN M(q)) ⇒ ∀q ∈ Q (R(q) ⇒∃qm ∈ Qm (q k qm)) . Let q ∈ Q and assume R(q) to reduce the proof to MMN M(q) ⇒ ∃ qm ∈ Qm (q k qm).Assume MMN M(q), or by Definitions 7 and 9: ∀M ∈M(q ∈ M ⇒ ∃ qm ∈ Qm (q M qm)) . Thus, for every 

M ∈ M and q ∈ M , it holds that ∃qm ∈ Qm (q M qm).By the construction of M , then also ∃qm ∈ Qm (q k qm).The opposite implication does not hold, because QN k(P )

does not imply that all states q ∈ M are modal nonblocking for every M ∈ M .VII. C ONCLUSION 

This paper has introduced modal and multimodal non-blocking to capture a more expressive and versatile non-blocking property. The novel variants can be applied to systems with fault behavior, reconfiguration and multiple control goals. Synthesis algorithms are presented to obtain modal or multimodal nonblocking supervisors. The novel variants are compared to established nonblocking variants. With modal and multimodal nonblocking, supervisory control synthesis could be sensibly applied to safety-critical water infrastructures with multiple control goals, like in [7]. This does require some implementation of the novel nonblocking variants in a tool like ESCET [12]. Additionally, for practical applications of the novel non-blocking variants, it is necessary that the supervisor is also 

safe : the supervisor never violates a given requirements model. The safety property is left out in this paper, but could be introduced by adding a plantified requirement model, see [13]. Synthesis of the synchronous composition [2] of the plant model and the plantified requirement model produces a safe supervisor by default. The hypothesis is that this still holds in the context of modal nonblocking, as long as the sink state belongs to at least one M .Furthermore, since EFAs can be transformed to FAs, as shown in [14], the modal and multimodal nonblocking property can be defined for EFAs as well. The expansion to EFAs is future work. Finally, there is potential for combining different, comple-mentary nonblocking variants in a real case study. Specifi-cally, progressive nonblocking, multitasking nonblocking and multimodal nonblocking could be combined to achieve a very versatile way to obtain very specific nonblocking properties. REFERENCES [1] P. J. Ramadge and W. M. Wonham, “Supervisory Control of aClass of Discrete Event Processes,” SIAM Journal on Control and Optimization , vol. 25, no. 1, pp. 206–230, 1987. DOI: 10.1137/0325013 [2] C. G. Cassandras and S. Lafortune, Introduction to Discrete Event Systems , 2nd ed. Boston, MA: Springer US, 2008. [3] R. Malik and R. Leduc, “Generalised nonblocking,” WODES , pp. 340– 345, 2008. DOI: 10.1109/WODES.2008.4605969 [4] S. Ware and R. Malik, “Progressive events in supervisory control and compositional verification,” Control Theory and Technology , vol. 12, no. 3, pp. 317–329, 2014. DOI: 10.1007/S11768-014-4097-8 [5] M. H. De Queiroz, J. E. Cury, and W. M. Wonham, “Multitasking supervisory control of discrete-event systems,” Discrete Event Dynamic Systems: Theory and Applications , vol. 15, no. 4, pp. 375–395, 2005. DOI: 10.1007/S10626-005-4058-Y [6] R. Zhang, J. Wang, Z. Wang, and K. Cai, “Quantitatively nonblocking supervisory control of discrete-event system,” Automatica , vol. 170, p. 111879, 2024. DOI: 10.1016/j.automatica.2024.111879 [7] M. Minkenberg, H. J. W. van Dal, and M. A. Goorden, “Abstraction for supervisor synthesis: Industrial application and insights,” to appear. [8] A. Arnold, Finite transition systems: semantics of communicating sys-tems . Englewood Cliffs, NJ: Prentice-Hall, 1994. ISBN: 0130929905 [9] L. Ouedraogo, R. Kumar, R. Malik, and K. ˚ Akesson, “Symbolic ap-proach to nonblocking and safe control of extended Finite Automata,”                                                            

> CASE , pp. 471–476, 2010. DOI: 10.1109/COASE.2010.5584659 [10] S. Thuijsman and M. A. Reniers, “Transformational supervisor syn-thesis for evolving systems,” Discrete Event Dynamic Systems: The-ory and Applications ,vol. 32, no. 2, pp. 317–358, 2022. DOI: 10.1007/S10626-021-00354-0 [11] ——, “Supervisory Control for Dynamic Feature Configuration in Product Lines,” ACM Transactions on Embedded Computing Systems ,vol. 23, no. 5, 2024. DOI: 10.1145/3579644 [12] W. J. Fokkink, M. A. Goorden, D. Hendriks, D. A. van Beek, A. T. Hofkamp, F. F. H. Reijnen, L. F. P. Etman, L. Moormann, J. M. van de Mortel-Fronczak, M. A. Reniers, J. E. Rooda, L. J. van der Sanden, R. R. H. Schiffelers, S. B. Thuijsman, J. J. Verbakel, and J. A. Vogel, “Eclipse ESCET™: The Eclipse Supervisory Control Engineering Toolkit,” TACAS , pp. 44–52, 2023. DOI: 10.1007/978-3-031-30820-8 6 [13] H. Flordal, R. Malik, M. Fabian, and K. ˚ Akesson, “Compositional syn-thesis of maximally permissive supervisors using supervision equiv-alence,” Discrete Event Dynamic Systems: Theory and Applications ,vol. 17, no. 4, pp. 475–504, 2007. DOI: 10.1007/S10626-007-0018-Z [14] M. Sk¨ oldstam, K. ˚Akesson, and M. Fabian, “Supervisory Control Applied to Automata Extended with Variables-Revised,” Chalmers University of Technology, Tech. Rep., 2008. Available: https: //research.chalmers.se/publication/65593/file/65593 Fulltext.pdf

8